(define (product term a next b)
  (if (> a b)
      1
      (* (term a)
         (product term (next a) next b))))
(define (indentity x) x)
(define (inc x) (+ x 1))
(product indentity 3 inc 4)
(define (factorial n)
  (product indentity 1 inc n))
(factorial 3)
(define (divide a)
  (* (/ a (+ a 1))
     (/ (+ a 2) (+ a 1))))
(define (divide-b a)
  (/ (* a (+ a 2))
     (* (+ a 1) (+ a 1))))
(define (pi n)
  (define (next a)
    (+ a 2))
  (* 4  (product divide-b 2 next n)))
(pi 12)
