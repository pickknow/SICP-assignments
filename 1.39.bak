#lang racket
(define fi 1.618)
(define (cont-frac n d k)  
    (define (try x)
      (if (> x k)
           (d k)
          (+ (d k) (/ (n (+ k 1)) (try (+ x 1))))))
  (/ (d 0) (try 1)))
(cont-frac (lambda (i) 1.0)
           (lambda (i) 1.0)
           9)
